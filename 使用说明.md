# 📖 CF 团队助手 v2.13 · 使用说明

> 一个**本地运行**的 Codeforces 辅助工具：查队友 Rating / 冲分进度、预测比赛涨跌分、定位能力短板、
> 按条件出题、组队作战分工、双人对比、题单收藏。打开即用，关掉网页自动退出。
>
> 数据全部来自 Codeforces 官方公开接口，不需要登录，不碰你的账号密码。

---

## 一、快速开始

### 方式 A：用打包好的 exe（推荐给没装 Python 的队友）

1. 双击 `dist\cf辅助工具.exe`（需先在项目根目录双击一次 `build.bat` 生成）。
2. 稍等 1~2 秒，浏览器会自动打开 [http://127.0.0.1:5000](http://127.0.0.1:5000) 。
3. **全程没有黑窗口**，任务栏里也看不到，这是正常的。
4. 不用时**直接关掉网页**，进程几秒后自动退出。

> **想带上已有数据**：把 `data\cf_army.json`（大部队）和 `data\cf_todo.json`（题单）
> 复制到 exe 同级的 `data\` 目录下即可。exe 首次运行会自己建这个目录。

### 方式 B：源码运行（自己开发 / 调试）

```bash
pip install -r requirements.txt
python run.py
```

⚠️ 一定要用**装了依赖的那个 Python**。本机 PATH 上的 `python` 是 msys2 版（3.14，已装 Flask / requests）；
Git Bash 里的 `/usr/bin/python` 没装依赖，会报 `No module named 'flask'`。

> **打包 exe 请用 `py -3`（原生 CPython）**，`build.bat` 已经这么做了。用 msys2 的 python 打出来的 exe
> 会依赖 mingw DLL，在别人电脑上跑不起来。详见 [README.md](README.md)。

---

## 二、顶栏：五个页面

|页面|路径|一句话|
|-|-|-|
|🏠 **首页**|`/`|查人：Rating、涨跌预测、热力图、弱点墙、训练计划、推荐题|
|🎲 **刷题器**|`/picker`|按「难度 + 标签 + 排除某人已做过」从全站题库出题|
|🏆 **ICPC**|`/icpc`|历年 ICPC / CCPC 比赛库，四档分级，自动标出你做过哪些题|
|🤝 **组队作战**|`/team`|一支队伍的技能矩阵、比赛分工、共同弱项、合练题单|
|🆚 **对比**|`/vs`|两个人 head-to-head 全方位对比|
|⭐ **题单**|`/todo`|收藏的待做题，顶栏带数量徽标|

右上角 **🌙 / ☀️** 切换深色 / 浅色主题。**默认深色**；切换后的选择会记住（存在浏览器 localStorage），
换浏览器或清了缓存则回到深色。浅色是暖纸色（卡片亮度约 73%，纯白为 100%），刻意压得比常见浅色主题暗，不会白得刺眼。

---

## 三、🏠 首页

### 1. 🪖 我的大部队

常看的队友存在这里，**跨会话保留**（存于 `data/cf_army.json`），启动时会自动刷新一次分数。

* **加入**：在下面搜索框查完某人后，点绿色 **➕ 加入大部队**。
* **🔄 刷新数据**：重新拉所有队友最新 Rating（会绕开缓存拿最新分）。
* 每张卡片：**📊 详情** = 直接查这个人；**❌ 移出** = 从大部队删掉。
* 大部队成员会在 🤝 组队作战 / 🆚 对比 页变成**快捷 chip**，点一下就填进输入框。

### 2. 📅 未来 30 天比赛日历

* 顶部按分区**筛选**（全部 / Div.4 / Div.3 / Div.2 / Div.1 / Div.1+Div.2 / Educational / Global）。
* 每场显示开始时间、时长、**实时倒计时**，点「前往 →」直达赛页。
* 查询某人之后，匹配 ta 等级的比赛会带 **🎯 适合你** 徽章。
* 比赛列表缓存 1 小时（`data/cf_upcoming_contests.json`），删了会自动重建。

### 3. 🔍 查询 + 预测

* 输入框填**一个** CF 用户名，例：`tourist`。首页专注单人深度分析。
* 右边下拉选**比赛分区**——这只影响 Rating 预测用哪套校准数据，选你打算打的那种。
* 点 **🔍 查询 + 预测**。查不到的用户名会在上方红色提示。

> **想同时看多个人？** 首页不做这件事，各有专门的页面更好用：
> 两人 head-to-head 用 **🆚 对比**，整队分工与合练用 **🤝 组队作战**，
> 历年 ICPC 战绩用 **🏆 ICPC**（这三个页面都支持多人）。
> 在首页填了多个名字时，会只查第一个并给出蓝色提示告诉你该去哪。

### 4. 查询结果

顶部是一行**数据总览**（Rating / 等级 / 提交 / AC / 正确率 / 各难度档 TOP 标签），
可 **⬇️ 导出 CSV**；「最近比赛」那格点一下能展开完整历史。

下面是**详情卡**，从上到下依次是：

|模块|看什么|
|-|-|
|**冲分进度条**|距下一档还差多少分|
|**📈 Rating 走势图**|等级色带背景 + 每场数据点，**悬停**看该场比赛名 / 排名 / `旧→新` 分 / 日期|
|**🔥 近一年热力图**|GitHub 风格年视图，强度 = **当天新解出的题数**；配套解出总数、活跃天数、当前 / 最长连刷、最高产一天。悬停某格看当天详情|
|**🎯 Rating 预测表**|不同 AC 题数 → 预估排名 / 百分位 / 表现分 / 涨跌分 / 赛后等级|
|**📊 难度分布直方图**|11 级难度各解出多少题，以及各档的擅长标签|
|**🧱 弱点墙**|见下|
|**📋 三分化训练计划**|舒适 / 提升 / 挑战三区 + 🔖 提升区推荐题|
|**🕐 近 90 天标签表现**|每个标签的正确率、WA / TLE 数|

### 5. 🧱 弱点墙（重点）

按 11 个难度档统计「**解出 / 尝试**」的**真实通过率**——注意分母**含没解出的尝试**，所以会比
「难度分布」里的纯 AC 计数低，这才是你的真实水平。

* **稳固上限**：通过率 ≥ 50% 的最高档，你还能稳定拿下的难度。
* **← 你的墙**：紧挨着上限往上的那一档，你开始崩的地方，**rating 想涨最该死磕的难度**。
  （它的通过率必然 < 50%——否则它自己就成上限了。）
* **某档至少尝试过 4 题，通过率才会被采信**，上限和墙都受这条约束。
  所以某档只碰过一两题、还全没做出来时，程序**不会**急着把它判成你的墙，而会提示你先去多试几题。
* 卡片上的 **🎯 去刷题器练这一档** 会带上你的墙难度区间 + 近况弱项标签，直接跳到刷题器**自动出题**。
这是从「知道问题」到「开始解决」最快的一步。

### 6. 📋 三分化训练计划

给出舒适区 / 提升区 / 挑战区三个难度区间，以及对应比赛的哪些题位；
**🔖 提升区推荐题目**会自动挑**你没做过**、且**命中你薄弱标签**的题（默认 8 道），难度均匀采样，
点卡片直达题面，点 **☆** 收进题单。

> **一个容易困惑的地方**：「近 90 天标签表现」卡片里 🟢 绿色是正确率 ≥70%、🔴 红色是 ≤30%，
> 那只是**卡片自己的配色**；而训练计划判定**强项**要 ≥75%、**薄弱**要 <40%（且该标签近 90 天尝试 ≥3 题）。
> 所以一个 72% 的标签会显示绿色，却不会被列进强项——两套阈值不同，不是 bug。

---

## 四、🎲 刷题器（`/picker`）

在全站所有**带难度分**的题里按条件出题。赛前热手、专项突破、给队友配题都好用。

1. 设 **难度区间**（默认 800~1600，步长 100）、**出题数量**（默认 12，最多 50）、
**排序**（随机抽取 / 按难度升序）。
2. **排除某人已做过**：填用户名（一般填自己），留空则不排除。
3. **点选标签**（可多选，**命中任一即可**；不选 = 全部）。
4. 点 **🎲 出题**。结果标题会显示「抽取 N 道 / 命中题库 M 题」——**M 很小说明筛得太窄**，
放宽难度或少选几个标签；M 很大说明筛得太宽。
5. 点卡片直达题面，点 **☆** 收进题单。

底部附 OI Wiki / A2OJ Ladders / USACO Guide 直链。

> 刚启动几秒内可能提示「题库仍在后台加载」，等几秒重试即可（题库在后台预热）。

**URL 可以直接预填**，方便存书签或发给队友：

```
/picker?rating_min=1400&rating_max=1600&tags=dp,greedy&count=12&mode=random&exclude=tourist&auto=1
```

`auto=1` = 打开就自动出题。标签含空格（如 `two pointers`）也能正确传递。

---

## 四点五、🏆 ICPC 比赛库（`/icpc`）

收录 Codeforces 上**以中国赛事为主**的 ICPC / CCPC 体系比赛，目前 **160 场**，按级别分四档：

|档位|收录内容|场次|
|-|-|-|
|**邀请赛 / 省赛**|各省省赛、CCPC 国邀、选拔性质赛事|57|
|**区域赛**|ICPC Asia 各站（南京/上海/杭州/成都/沈阳…）、CCPC 分站与总决赛|83|
|**EC-Final**|ICPC 亚洲东部大陆决赛|6|
|**World Finals**|ICPC 全球总决赛|14|

### 怎么用

1. 直接打开就是一份可浏览的比赛库，点比赛名直达 CF。
2. **填用户名 → 自动标出你做过哪些题**。做过的比赛整行会绿色高亮，右侧显示解出的题号。
3. **逗号分隔填多个人 = 全队合并视图**：显示全队并集解出多少题，以及每题是谁做出来的——
   赛前想知道「这场我们队集体啃过没有」很好用。
4. 顶部四张卡是**战绩总览**：每一档参与过几场、累计解出多少题。
5. 筛选：档位 chip + 年份下拉 + 关键词搜索（中英文都行，如 `南京` 或 `Nanjing`）+「只看做过的」。

> **Gym 提交也算得进来**。历年区域赛真题绝大多数在 CF 的 Gym（练习场）里，
> 而 CF 的提交记录接口是包含 Gym 的，所以你在 Gym 上补的题这里能自动对上，无需手工登记。

### 🎲 抽一场来练

选一个档位点 **🎲 抽取**，直接给你一场该练的比赛，附完整题目网格（可点进去）。

抽取规则：

1. **完全没解出过题的场次优先**——只要该档还有这样的场次，就只从它们里面抽；
2. 该档每场都解出过题了，则剩下的**一视同仁**、等概率抽；
3. **已解出超过 60% 的场次不会被抽到**——练它的边际收益太低（阈值见 `ICPC_PICK_SKIP_RATE`）。

> 两处边界：**「交过但一题没解出」仍算没做过**（那场对你依然是完整的练习量）；
> 题单未知的场次算不出比例，**不能证明超线**，所以保留在池子里。
> 不填用户名时视为全部没做过，等同纯随机抽。

某一档所有比赛你都做过 60% 以上时，会提示你换个档位——那是好消息。

### 逐题状态

每场比赛下面是一排**题号方块**，点任意一个直达题面，悬停显示题名（官方赛还有难度）：

|颜色|含义|
|-|-|
|🟢 绿色|已解出|
|🔴 红色|**交过但始终没做出来**——你卡住的题，最值得回头攻|
|⬜ 灰色|还没碰过|

配合 `10/11`、完成率百分比和进度条，一眼看出这场啃到什么程度。全队视图下悬停还会显示是谁做出来的。

### 精确完成度需要 CF API 密钥（可选）

Gym 比赛的题目列表 CF 要求认证才能取，所以默认只能显示「已解出 3 题」而没有分母。
配好密钥后可显示精确的 `3/13`：

1. 去 [codeforces.com/settings/api](https://codeforces.com/settings/api) 点 **Add API key**，得到 key 与 secret。
2. 在 `data\` 下建 `cf_api.json`：`{"key": "你的key", "secret": "你的secret"}`。
3. 跑一次 `python tools\verify_cf_key.py` 确认可用。
4. 打开 🏆 ICPC 页，点 **📥 抓取题单**（约 4 分钟，**只需一次**，之后永久缓存）。

> 密钥只存在你本机的 `data\` 下，已被 `.gitignore` 排除，不会进版本库，也不会发给任何第三方。
> 不配密钥的话其它功能完全不受影响，只是 Gym 场次没有分母。

### 两点说明

- 比赛库缓存 **7 天**（历年赛事几乎不变）。新赛季比赛出来后，点右上角 **🔄 重拉比赛库** 立即更新。
- 极少数受限赛事（如邀请制的 JetBrains 专场）CF 对任何人都不开放榜单，这类会一直显示「已解出 N 题」，
  程序不会反复去重试它们。

---

## 五、🤝 组队作战板（`/team`）

1. 输入队员用户名（ICPC 3 人，**最多 6 人**），或点大部队 chip 快速填入。
2. 点 **🤝 生成作战板**，得到：

   * **🧭 比赛分工建议**：按擅长标签给出「谁开哪类题」；
   * **🧱 标签技能矩阵**：全队解题数 TOP 16 的标签下每人各解出多少，🏆 标出全队最强；
   * **全队共同弱项**：连最强的人都做得很少（< 4 题）的标签，建议一起合练；
   * **合练题单**：**全队都没做过**的题，点 ☆ 可收藏。

---

## 六、🆚 对比（`/vs`）

两名选手 head-to-head，**A 蓝 / B 橙**贯穿全页。填两个用户名（或点大部队 chip 依次填 A、B）。

* **⚔️ 同场交锋战绩**：两人都参加过的比赛逐场比名次（名次小者胜），给出 `A x:y B` 的真实比分

  * 最近交锋逐场列表。**这是最有意思的一块**——不是比分数，是比真刀真枪同场较量的结果。
* **📈 双线 Rating 走势同图**：两人画在同一张图上，一眼看出谁何时领先、交叉点在哪。
* **📊 核心数据并排**：当前 / 最高 Rating、总 AC、正确率逐行对比，🏆 高亮赢家。
* **🧱 难度分布背靠背** + **🏷️ 标签强弱对比**：谁在哪类题更强。
* **🧩 已解题集合**：共同做过 / 只有 A 做过 / 只有 B 做过——**定位可以互相取经的方向**。

> 两人没有同场比赛记录时会显示「无同场参赛记录」，其它模块照常。

---

## 七、⭐ 题单（`/todo`）

在**推荐题**、**🎲 刷题器**、**🤝 合练题单**的卡片上点 **☆** 即可收藏，**跨会话保留**
（存于 `data/cf_todo.json`）。顶栏 ⭐ 题单带**数量徽标**。

* 待做 / 已完成**分区展示**；
* **✓ 完成** / **↺ 标为待做** 切换状态；**✕ 移除** 删掉；
* **🧹 清空已完成** 一键清理。

> 题目按 `key`（`contestId_index`，如 `4_A`）唯一去重，同一题收藏两次不会重复。
> 题目链接由服务端根据 key 重建，不采信前端传来的 URL。

---

## 八、一条典型的使用路径

把各页面串起来，这是最能出效果的用法：

1. **首页**查自己 → 看 **🧱 弱点墙**，找到「你的墙」是哪一档；
2. 点 **🎯 去刷题器练这一档** → 自动带上墙难度 + 弱项标签出题；
3. 看不顺眼的题点 **☆** 收进 **⭐ 题单**，之后按题单慢慢刷；
4. 刷一阵子后回首页看 **🔥 热力图** 和弱点墙，确认墙有没有往上移；
5. 赛前用 **🤝 组队作战** 定分工，用 **🆚 对比** 看看和队友/对手的差距在哪类题上。

---

## 九、⚠️ 注意事项

1. **必须能访问 codeforces.com**。CF 是海外站点，国内网络可能很慢或连不上，必要时自备代理 / 加速。
**日历或查询为空，绝大多数是网络问题**，不是程序坏了。
2. **关网页 ≈ 关程序**。页面每 5 秒自动发一次心跳，所以**只要还开着，晾多久都不会被误杀**。
关掉所有相关标签时浏览器会主动通知服务端，进程 2~4 秒内退出；只关一个、还有别的标签开着**不会**退出，
在顶栏几个页面之间点来点去也**不会**（应用内跳转不算关页）。
兜底：连续 30 秒收不到任何心跳（比如浏览器崩了、通知没发出去）也会退出。
3. **首次 / 查询会有点慢**。每查一个用户要请求 3 个 CF 接口（资料 / 提交 / Rating），叠加海外延迟属正常。
同一个用户 5 分钟内重复查会直接命中缓存，很快。
4. **推荐题目偶尔为空 = 正常**：要么你在该提升区已经几乎做完（高分选手常见），
要么程序刚启动、题库缓存还在后台加载（等几秒重查）。
5. **数据文件都在 `data\`**：

   * `cf_army.json` = 大部队，**想保留就别删**，换电脑 / 换 exe 复制它即可；
   * `cf_todo.json` = 题单，同上；
   * `cf_upcoming_contests.json` = 比赛缓存（1 小时过期），**可随意删**，删了自动重建；
   * 偶尔出现的 `*.tmp` = 写入用的临时文件，`*.bad` = 检测到文件损坏时自动留下的备份，都可删。
     **看到 `.bad` 说明那个文件坏过**，想抢救就把它改回原名再手工修 JSON。
6. **预测是估算值**。基于 CF 官方 Elo 算法 + 各分区历史数据校准，用于判断方向，别当精确值。
7. **端口固定 5000**。被占用就先关掉占用的程序，或改 `run.py` 里的 `PORT`。
8. **用户名大小写不敏感**，但要拼写正确。

---

## 十、常见问题 FAQ

**Q：双击 exe 没反应 / 浏览器没打开？**
A：单文件 exe 没有控制台，**出错不会有任何提示**。多半是被杀毒软件拦了（首次启动要解压到临时目录，
容易被误报），或 exe 所在目录没有写入权限。把 exe 放到非系统盘（如 D 盘）再试；
或者用源码方式 `python run.py`，能直接看到报错。

**Q：浏览器没自动打开，但程序好像在跑？**
A：手动在浏览器输入 [http://127.0.0.1:5000](http://127.0.0.1:5000) 即可，功能完全不受影响。

**Q：想立刻退出程序，不想等？**
A：关掉网页后等几秒即可。实在要立刻退，去任务管理器结束 `cf辅助工具.exe`。

**Q：近 90 天标签表现一片空白？**
A：需要某标签近 90 天**尝试 ≥ 3 题**才会展示。新号或最近没怎么刷题就会很少 / 没有。

**Q：热力图 / 弱点墙和我印象里的数不一样？**
A：热力图强度是「**当天新解出的题数**」（每题按**最早一次 AC** 落到那天，按北京时间划天），
重复提交同一题不会重复计数。弱点墙的分母含没解出的尝试，所以通过率比纯 AC 计数低，这是有意为之。

**Q：想改「未来多少天比赛」「多少秒自动退出」「推荐几道题」这些？**
A：改 `cfhelper\config.py` 顶部的常量，改完重启。常用的几个：

|常量|默认|含义|
|-|-|-|
|`UPCOMING_DAYS`|30|比赛日历往后看多少天|
|`HEARTBEAT_TIMEOUT`|30|多少秒收不到心跳就退出|
|`SHUTDOWN_GRACE`|2|收到关闭信标后的宽限秒数，留给应用内跳转的新页面来心跳|
|`RECOMMEND_LIMIT`|8|推荐几道题|
|`UNRATED_PLAN_RATING`|1200|未评级账号（没打过 Rated 比赛）的训练计划按哪个分数规划|
|`HEATMAP_DAYS`|365|热力图回看多少天|
|`RECENT_DAYS`|90|近况标签统计窗口|
|`WEAK_AC_RATE` / `STRONG_AC_RATE`|40 / 75|训练计划判薄弱 / 强项的正确率线|
|`WALL_WALL_RATE`|50|弱点墙判稳固上限 / 墙的通过率分界线|
|`WALL_MIN_ATTEMPTS`|4|某档至少尝试几题，通过率才被采信|
|`PICK_DEFAULT_COUNT` / `PICK_MAX_COUNT`|12 / 50|刷题器默认 / 最多出几题|
|`TEAM_MAX_SIZE`|6|组队作战最多几人|
|`USER_CACHE_TTL`|300|用户数据缓存秒数|
|`HTTP_MAX_CONCURRENCY`|4|同时在飞的 CF 请求数上限（调高易被 CF 限流）|

**Q：怎么自测有没有改坏？**
A：`python tests\smoke_test.py`（加 `--net` 会连真实接口测）。当前 **20 项**，全过才算稳。

---

## 十一、相关文档

* 架构 / 开发 / 打包说明见 [README.md](README.md)
* 版本改动 / Bug 修复见 [CHANGELOG.md](CHANGELOG.md)
* v1.x 历史源码存档在 [历史版本/](历史版本/)
