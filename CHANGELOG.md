# 更新日志

## v2.7 —— 修 Bug 与健壮性专版

> 本版没有新功能，全部用来修此前埋着的正确性、数据安全与文档问题。冒烟测试 17 → **24 项**，
> 每个修复都配了对应的回归守护。

### 🐞 会算错 / 算不出的

- **未评级与低分用户的训练计划全废**：CF 对未评级账号不返回 `rating`，上游填 0，
  训练区间算出 `800 ~ -200` 这种**倒挂区间**，推荐池 `lo > hi` 恒为空——一道题也推不出来。
  实测 **rating < 1000 的真实用户同样倒挂**，1000 分退化。现在区间上下界都做钳位，
  未评级按 `UNRATED_PLAN_RATING`（1200）规划并在页面上说明。
  用真实未评级账号验证：修复前推荐 0 道，修复后 8 道。
- **弱点墙深链按钮对顶级用户失效**：无墙时用 `ceiling.max + 1` 算目标难度，
  而最高档的 `max` 是 9999（分档开口，题库实际只到 3500），于是深链成 `10000~10199`，
  **点过去必然出 0 题**。现在钳在题库真实范围内；已练到顶档时回退为在该档继续加量
  （实测 tourist：修复前 0 题 → 修复后 988 题）。
- **「墙」的判定不看样本量**：`WALL_MIN_ATTEMPTS` 只约束了稳固上限，找墙的条件是
  `attempted > 0`，导致**只碰过 1-2 题的难度档也会被斩钉截铁地判成你的墙**。
  现在上限与墙同受该约束；定不出墙时结论会说明原因，而不是含糊地说"几乎没碰过"。

### 🛡️ 数据安全

- **大部队 / 题单可能被静默清空**：写文件用的是 `open(path, "w")` 直接截断覆写，
  而心跳看门狗用 `os._exit(0)` 硬退出——两者相撞就留下残缺 JSON，
  而 `load()` 的 `except` 会把它当成空表，下一次保存就把空表写死。
  改为**临时文件 + `fsync` + `os.replace` 原子写**（`paths.atomic_write_json`）。
  实测在写入途中硬退出各 25 次：旧写法残缺 **24** 次，新写法 **0** 次。
- **损坏不再无声无息**：`paths.read_json` 区分"文件不存在"（正常）与"文件损坏"，
  后者备份成 `.bad` 并告警，不让下一次保存把它覆盖掉。
- **心跳竞态**：顶栏跳转等应用内导航也会触发 `pagehide` 发关闭信标，
  而旧版把心跳直接置 0，看门狗可能在新页面加载完之前**把进程杀在跳转途中**。
  现在留 `SHUTDOWN_GRACE`（2 秒）宽限，任意一次心跳都能取消退出；
  前端另加站内链接标记，正常跳转连信标都不发。代价是关页退出从 ≤2 秒变为 2~4 秒。
- **刷新大部队会冲掉并发改动**：拉取那几秒里的增删会被开头的快照覆盖，现在改为重读后按 handle 合并。

### 🔒 跨站请求

- 改状态的端点（`/shutdown`、`/api/add_army`、`/api/remove_army`）此前**接受 GET**，
  任意网页一个 `<img src="http://127.0.0.1:5000/shutdown">` 就能关掉你的程序，
  `remove_army` 同理能删队友。现在全部 POST-only，并加同源守卫拒绝跨站 `Origin`。
- 顺带统一了接口风格——此前题单 API 是 POST-only，大部队 API 是 GET，两套约定。

### 🧹 一致性与清理

- **`使用说明.md` 文件损坏修复**：全文 0 个换行符、267 个裸 CR、586 个反斜杠
  （`**` 被反复转义成 `\\\\\\\*\\\\\\\*`），多数渲染器里是一坨无法阅读的文本。
  已按原结构还原并统一为 LF。
- **弱点墙阈值文档与代码不符**：文档写"稳固上限 ≥80%"，代码实际用 50%，
  而 `WALL_SOLID_RATE=80` 从未被引用。改为单阈值设计并删除该常量——
  修完样本量问题后这套设计本就自洽：墙是上限之上第一个可信档，其通过率必然 < 50%。
- **缓存只增不减**：`_user_cache` 只在读取时判过期、从不删除条目，而 `subs` 存的是完整提交记录。
  新增 `USER_CACHE_MAX`（30 条 ≈ 10 个用户），写入时先清过期再按时间淘汰。
- **测试污染真实数据**：`test_render_offline` 会把 0 场比赛写进真实的比赛缓存，
  害得之后一小时首页日历全空；且它猴补丁 `get_contest_list` 后不还原，
  连带让实网测试也是拿空日历渲染的。现已改用临时文件并在 `finally` 里还原。
- 删除死代码 `cf_api.clear_user_cache()` 与 `generate_training_plan` 的 `total_tags` 死参数。
- 模板里硬编码的"尝试 ≥3 题"改为引用 `MIN_RECENT_PROBLEMS`；
  多用户查询时比赛日历的「🎯 适合你」改为取所有已查询用户的并集，不再只看第一个人。

## v2.6 —— 题单 / 待做收藏

- **⭐ 题单**（`/todo`）：在推荐题、🎲 刷题器、🤝 合练题单的卡片上点 ☆ 即可收藏，**跨会话保留**（存于 `data/cf_todo.json`）。
- 题单页可标记**完成 / 待做**、移除、一键**清空已完成**；待做与已完成分区展示；顶栏 ⭐ 题单带**数量徽标**。
- 安全：题目以 `key`（contestId_index）唯一去重，**URL 由 key 服务端重建**（不信任前端传入链接）；增删改用锁串行化（`cfhelper/todo.py`）。
- 推荐 / 刷题结果新增 `key` 字段供收藏；冒烟测试扩到 **17 项**（含题单 CRUD + `/todo` 离线渲染）。

## v2.5 —— 刷题活跃热力图

- **🔥 近一年刷题热力图**（首页用户详情）：GitHub 风格年视图，强度 = **当天新解出的题数**（每题按最早 AC 落日、按 CST 划天）。
  配套统计：解出总数、活跃天数、**当前连刷 / 最长连刷**、最高产一天。服务端直接渲染网格（原生 `title` 悬停看某日详情，零额外 JS）。
- 计算核心 `analysis.activity_heatmap`（周一对齐的周列网格 + 月份标签 + 连刷统计）；窗口由 `config.HEATMAP_DAYS=365` 控制。冒烟测试扩到 **16 项**。

## v2.4 —— 个人 vs 队友 head-to-head 对比页

> 新增 `/vs` 对比页：两名选手全方位 head-to-head，A 蓝 / B 橙贯穿全页。

- **⚔️ 同场交锋战绩**：两人都参加过的比赛逐场比名次（名次小者胜），给出 `A x:y B` 真实胜负记录 + 最近交锋逐场列表。
- **📈 双线 Rating 走势同图**：两人走势画在同一张图上（等级色带背景 + 共享时间轴 + 悬停看每场详情），一眼看出谁何时领先、交叉点在哪。
- **📊 核心数据并排**：当前 / 最高 Rating、总 AC、正确率逐行对比，🏆 高亮赢家。
- **🧱 难度分布背靠背** + **🏷️ 标签强弱对比**（谁在哪类题更强）+ **🧩 已解题集合**（共同 / 只有 A / 只有 B，定位可互相取经的方向）。
- 计算核心 `analysis.compare_users`；前端 `vs.js` 复用 app.js 的色带 / 提示 / Toast；同名、单方失败均有兜底。冒烟测试扩到 **15 项**。

## v2.3 —— 闭环成「可行动」+ 健壮性补强

> 把"分析"接上"行动"：看到弱点墙后一键就能去刷对应难度/弱项的题；同时补齐并发与输入的健壮性短板。

### ✨ 打通闭环
- **弱点墙 → 刷题器一键深链**：弱点墙卡片新增「🎯 去刷题器练这一档」按钮，自动带上**你的墙难度区间**
  （`target_min/target_max`）+ **近况弱项标签**跳转 `/picker` 并自动出题。
- **刷题器支持 URL 预填**：`/picker?rating_min=&rating_max=&tags=&count=&mode=&exclude=&auto=1`，
  深链进来自动填表并出题；标签含空格（如 `two pointers`）也能正确 round-trip。
- **刷题器显示命中总数**：结果标题显示「抽取 N 道 / 命中题库 M 题」，立刻知道筛选是太宽还是太窄。

### 🛡️ 健壮性
- **单人出错隔离**：并行查询里任何一个 handle 分析异常都只算"查询失败"，**不再连累其他人或 500 整页**。
- **用户名格式校验**：首页 / 组队 / 刷题器排除项统一用 `cf_api.is_valid_handle` 提前挡掉非法输入，
  省掉注定失败的请求，错误提示更清晰。
- **大部队文件写锁**：增删 / 刷新的"读-改-写"用锁串行化，杜绝并发写坏 `cf_army.json`。
- 刷题器难度区间写反自动纠正。

### ⚡ / 🧪
- 弱点墙改**单遍扫描**桶计（不再每档重扫一遍提交）。
- 冒烟测试 10 → **14 项**：新增用户名校验、错误隔离、弱点墙目标分段、刷题器命中总数。

## v2.2 —— 并行提速 + 弱点墙 + 刷题器 + 团队作战板

> 站在 ICPC 选手视角补齐「个人 → 团队」这条线，并把查询慢的老问题从根上解决。

### ⚡ 性能（默认生效，无需配置）
- **查询并行化**：单用户的 info/status/rating 三接口并行拉取；多用户、批量刷新大部队也并行。
  原本 N 个用户 × 3 接口全程串行，现整体受**全局信号量**限流（`HTTP_MAX_CONCURRENCY=4`，避免触发
  CF "Call limit exceeded"）。实测两个重量级账号组队分析约 8 秒，串行需翻倍。
- **用户数据缓存**：info/status/rating 进程内 TTL 缓存（`USER_CACHE_TTL=300`），重复查询 / army 点
  详情直接命中；手动刷新大部队用 `force` 绕开缓存拿最新分。
- **限流退避**：`_get` 识别 CF 限流（comment 含 limit）会退避重试，业务错误（用户不存在）则立即返回。
- 修复深色模式刷新**白屏闪烁**（FOUC）：主题在 `<head>` 渲染前即确定。

### ✨ 新功能
- **🧱 弱点墙**：按 11 个难度档统计「解出 / 尝试（含没解出的）」真实通过率，自动定位你的
  **稳固上限**与**墙**（通过率骤降处），并给出该主攻哪一档的结论。
- **🎲 刷题器**（`/picker`）：在全站 1w+ 带分题里按 **难度区间 + 标签（多选）+ 排除某人已做过**
  随机 / 升序出题；附 cp-algorithms、CF EDU、A2OJ、USACO Guide 题型资源直链。
- **🤝 团队作战板**（`/team`）：输入 ICPC 队伍 → 并行分析 → **标签技能矩阵**（每类题谁最强）+
  **比赛分工建议**（谁开哪类题）+ **全队共同弱项** + **全队都没做过的合练题单**。

### 🧹 工程化
- 抽出 `base.html` 公共骨架（顶栏 / 导航 / 主题 / 遮罩 / Toast），index/picker/team 三页继承，去重。
- 新增 `picker.js`、`team.js`；`analysis` 增 `analyze_weakness_wall / pick_problems / analyze_team / solved_keys_of`。
- 冒烟测试从 5 项扩到 **10 项**，覆盖弱点墙、刷题器、团队分析，并新增**离线整页渲染**回归（守护模板继承与新路由）。

## v2.1 —— 走势图增强 + 关闭即退出

- **📈 Rating 走势详图**：把原来的迷你 sparkline 升级为大号交互图——带 Y 轴 Rating 刻度、
  CF 等级色带背景、X 轴日期、每场比赛数据点；**鼠标悬停**显示该场比赛名、排名、`旧→新` 分及涨跌、日期；
  宽度自适应、随窗口缩放重绘。
- **🛑 关闭网页即退出**：新增 `/shutdown`，页面卸载时用 `navigator.sendBeacon` 主动通知，
  进程 **≤2 秒**退出（旧版只能被动等 30 秒心跳超时）。并对"查询/刷新触发的页面跳转"做了标记，
  **不会误杀**进程；多标签场景下其它标签仍在心跳则自动取消退出。看门狗 30 秒超时仍作为兜底。

## v2.0 —— 模块化重构 + 修复 + 功能拓展

> 在 v1.2 单文件脚本基础上重写为标准模块化工程（`cfhelper/` 包 + 模板/静态分离 + 文档/打包脚本）。

### 🐞 Bug 修复
- **近 N 月标签正确率恒为 0**（即 `更新想法.txt` 里记的 1.3 问题）：旧版 `get_recent_tags_stats`
  把计算 `ac_rate` 的循环误删成一行注释 `# ... 后续计算ac_rate的代码保持不变 ...`，导致正确率永远是 0，
  连带训练计划的"薄弱/强项标签"判定全部失效。现按「解出题数 / 尝试题数」重写。
- **"最近比赛"永远显示"暂无"**：旧版请求的是**不存在的端点** `user.contests`，请求必然失败。
  已改为官方 `user.rating`，字段 `contestName / rank / oldRating / newRating`，最近比赛与走势图均可用。

### ⚡ 计算逻辑
- `user.status` 每个用户**只拉一次**，刷题统计与近况标签复用同一份数据（旧版重复请求两次）。
- 难度分级**优先用提交自带的 `problem.rating`**，缓存缺失时再回退题库缓存——更稳，缓存失败也能分级。
- 统一的请求封装：超时 + 轻量重试 + 异常兜底。

### ✨ 新功能
- **🔖 题目推荐**：提升区内自动挑选未做过、命中薄弱标签的题目，难度均匀采样，点击直达。
- **📈 Rating 走势**：用户详情内嵌迷你折线图（SVG）。
- **📊 难度直方图**：11 级难度已解题数量条形分布。
- **🎯 比赛"适合你"**：按当前查询用户等级，高亮匹配分区的比赛。
- **🔍 比赛筛选**：日历按 Div 分区一键筛选。
- **🌙 深色模式**、加载遮罩、Toast 提示、查询失败用户名提示、团队表导出 CSV、多用户锚点导航。

### 🧹 工程化
- 拆分为 `config / paths / cf_api / analysis / army / contests / routes` 七个模块 + 模板 + 静态资源。
- 新增 `README.md`、`requirements.txt`、`build.bat`、`.gitignore`。
- 路径解析兼容"源码运行"与"PyInstaller 打包"两种模式；运行数据统一放 `data/`。
- 旧版本（源码 1.0–1.2、最新版 exe、老版工具）归档至 `历史版本/`。

---

## v1.2（历史）
- 移除搜索历史，仅保留大部队功能。
- 关闭网页后进程自动退出（心跳机制）。
- 比赛日历窗口 15 天 → 30 天。
- 11 级精细化难度分档。
- 启动时刷新大部队数据。
- ⚠️ 遗留：近况标签正确率恒为 0（v2.0 修复）。

## v1.1（历史）
- 新增「大部队」收藏队友功能。
- 三分化训练计划、最近 3 个月标签统计、Rating 预测。

## v1.0（历史）
- 初版：团队 Rating 查看、刷题标签统计、比赛日历、搜索历史。
