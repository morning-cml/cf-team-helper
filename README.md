# ⚔️ CF 团队助手 v2.11

一个本地运行的 Codeforces 团队辅助工具：查看队友 Rating / 冲分进度、比赛 Rating 预测、
个性化训练计划与题目推荐、弱点墙、刷题器、ICPC 团队作战板、未来一个月比赛日历。
基于 **Flask**，打开即用，关掉网页自动退出进程。

> 在 v1.x 单文件脚本基础上重构为模块化工程，修复了两个隐藏 bug；v2.2 起把查询全程并行化并加缓存，
> 补齐「个人 → ICPC 团队」这条线（弱点墙 / 刷题器 / 团队作战板）；v2.3 把分析接上行动——看到弱点墙
> 一键去刷对应难度与弱项的题，并补齐并发 / 输入健壮性；**v2.7 是修 Bug 专版**，修掉未评级用户训练计划
> 失效、数据文件可能被静默清空、跨站请求能关进程等问题，冒烟测试扩到 24 项。详见 [CHANGELOG.md](CHANGELOG.md)。

> 📖 **只想知道怎么用（含发给队友）** → 看 [使用说明.md](使用说明.md)。本文档偏架构与开发。

> **© 2026 [morning-cml](https://github.com/morning-cml) · 依 [GPL-3.0](LICENSE) 授权**
> 欢迎自由使用、学习、修改。但**任何基于本项目的修改版，在对外分发时必须同样以 GPL-3.0 开源、
> 保留原作者署名、并声明改动**——不得据为己有或闭源发布。详见 [许可证与版权](#-许可证与版权)。

---

## ✨ 功能特性

- **🪖 大部队**：收藏关注队友，启动自动刷新 Rating，支持手动一键刷新。
- **📅 比赛日历**：未来 30 天 CF 比赛，实时倒计时，可按分区筛选，按你的等级高亮「🎯 适合你」。
- **🎯 Rating 预测**：基于 CF 官方 Elo 算法 + 各分区真实数据校准，预测不同 AC 题数下的排名与涨跌分。
- **📊 刷题画像**：11 级精细难度直方图、各难度档擅长标签、**Rating 走势交互图**（等级色带 + 悬停看每场详情）。
- **📋 三分化训练计划**：舒适 / 提升 / 挑战三区间，结合近况识别薄弱 / 强项标签。
- **🔖 题目推荐**：在「提升区」内自动挑选**未做过**、命中薄弱标签的题目，点击直达。
- **🧱 弱点墙**：按 11 个难度档算「解出 / 尝试」真实通过率，定位你的稳固上限与「墙」，**一键跳刷题器**按该难度+弱项出题。
- **🔥 刷题热力图**：GitHub 风格近一年活跃图（强度=当天新解出题数）+ 当前/最长连刷、活跃天数、最高产日。
- **🎲 刷题器**（`/picker`）：全站题库按 难度区间 + 标签 + 排除某人已做 随机/升序出题，附题型资源直链。
- **🏆 ICPC 比赛库**（`/icpc`）：收录 CF 上中国赛事为主的 ICPC / CCPC 体系比赛（160 场），按
  **邀请赛/省赛 → 区域赛 → EC-Final → World Finals** 四档分级；填入用户名自动标出你（或全队）
  在每场做出了哪些题（**Gym 提交也算得进来**），支持按档位 / 年份 / 关键词筛选与「只看做过的」；
  **🎲 抽一场来练**：按档位随机抽，没解出过题的场次优先、已解出 >60% 的排除。
- **🤝 团队作战板**（`/team`）：ICPC 队伍标签技能矩阵 + 比赛分工建议 + 全队共同弱项 + 全队合练题单。
- **🆚 对比页**（`/vs`）：两名选手 head-to-head——同场交锋胜负记录、双线 Rating 走势同图、核心数据/难度/标签强弱、已解题集合。
- **⭐ 题单**（`/todo`）：推荐题/刷题器/合练题单里点 ☆ 收藏，跨会话保留，可标记完成/移除/清空，顶栏带数量徽标。
- **🕐 近 90 天标签正确率**：以"解出题数 / 尝试题数"为口径，定位当前手感强弱。
- **⚡ 并行 + 缓存**：单人查询的 3 个接口并行拉取，组队 / 对比 / ICPC / 大部队刷新多人并行（全局信号量限流防 CF 封禁）+ 用户数据 TTL 缓存。
- **🌙 体验**：深色（默认）/ 浅色双主题（对比度按 WCAG 核算）、加载遮罩、Toast 提示、查询失败提示、数据总览导出 CSV。

> 首页专注**单人**深度分析；需要同时看多人时，用 🆚 对比（两人）/ 🤝 组队作战（整队）/ 🏆 ICPC（全队战绩）。

---

## 🚀 快速开始（源码运行）

```bash
pip install -r requirements.txt
python run.py
```

浏览器会自动打开 <http://127.0.0.1:5000> 。关闭网页后约 30 秒进程自动退出。

## 📦 打包成 exe

双击 `build.bat`（或手动执行其中的 PyInstaller 命令）。产物是单个 `dist/cf辅助工具.exe`，
绿色免安装，发给没装 Python 的队友也能用。运行时无黑窗口，关掉网页即退出。

打包必须用**原生 CPython**（`build.bat` 里走 `py -3` 启动器）：PATH 上的 `python` 是 msys2 版，
用它打出来的 exe 会依赖 mingw DLL，换台机器跑不起来。

---

## 📁 目录结构

```
cf辅助工具/
├─ run.py                 # 启动入口
├─ build.bat              # 一键打包 exe
├─ requirements.txt
├─ README.md / CHANGELOG.md
├─ cfhelper/              # 主程序包
│  ├─ __init__.py         # create_app 应用工厂 + 后台预热
│  ├─ config.py           # 所有常量：等级 / 难度分档 / 比赛校准 / 阈值 / 并发 / 缓存
│  ├─ paths.py            # 源码 & 打包两种模式下的路径解析
│  ├─ cf_api.py           # CF 接口封装（并发信号量、TTL 缓存、限流退避、并行 bundle）
│  ├─ analysis.py         # 计算核心：等级 / 统计 / 预测 / 训练 / 推荐 / 弱点墙 / 刷题 / 团队
│  ├─ army.py             # 大部队增删 / 去重 / 并行批量刷新
│  ├─ todo.py             # 题单 / 待做收藏（本地持久化、带锁、key 去重）
│  ├─ contests.py         # 比赛日历（拉取 / 缓存 / 解析 / 实时倒计时）
│  ├─ icpc.py             # ICPC 比赛库（gym+官方筛选 / 四档分级 / 完成度合并）
│  ├─ routes.py           # Flask 路由（首页 / 刷题器 / ICPC / 组队 / 对比 / 题单 / 大部队 API）+ 心跳
│  ├─ templates/          # base.html（公共骨架）+ index / picker / team / vs / todo
│  └─ static/{css, js}    # app.js（共享+首页）/ picker.js / team.js / vs.js / todo.js / style.css
├─ data/                  # 运行时数据：cf_army.json、比赛缓存
└─ 历史版本/              # v1.0–1.2 源码、旧 exe 归档
```

### 架构分层

```
routes  →  analysis / army / contests  →  cf_api  →  Codeforces API
                     ↑                        ↑
                  config ←──────────────── paths
```

`config` / `paths` 是无依赖的底座；`cf_api` 只负责网络与缓存；`analysis` 是纯计算；
`routes` 只做编排与渲染。各层职责单一，便于单独测试与修改。

---

## 🗂️ 数据文件

| 文件 | 说明 |
|---|---|
| `data/cf_army.json` | 大部队成员，建议保留 / 迁移 |
| `data/cf_todo.json` | 题单 / 待做收藏，建议保留 / 迁移 |
| `data/cf_upcoming_contests.json` | 比赛列表缓存（1 小时过期，可随意删除） |

打包后数据目录在 `exe` 同级生成。迁移大部队：把 `data/cf_army.json` 复制到目标的 `data/` 下。

---

## ❓ 常见问题

- **首次查询慢**：每个用户需请求 user.info / status / rating 三个接口（现已并行拉取），CF 海外接口本身有延迟，属正常；重复查询同一人会命中缓存秒出。
- **推荐题目为空**：你在该提升区已几乎做完（常见于高分用户），或题库缓存仍在后台加载（启动数秒内）。
- **比赛日历为空**：未来 30 天确实没有已公布的 Rated 比赛，或 CF 接口暂时不通。
- **进程没退出**：关掉网页标签后约 2~4 秒自动退出（关闭信标 + `SHUTDOWN_GRACE` 宽限期，宽限是为了不把应用内跳转误判成关页）；若浏览器拦截了信标，最迟 30 秒心跳超时也会退出。多标签时需关掉所有相关标签。

---

## ⚙️ 关键参数（`cfhelper/config.py`）

| 常量 | 默认 | 含义 |
|---|---|---|
| `UPCOMING_DAYS` | 30 | 比赛日历窗口 |
| `RECENT_DAYS` | 90 | 近况标签统计窗口 |
| `HEATMAP_DAYS` | 365 | 刷题热力图回看天数 |
| `HEARTBEAT_TIMEOUT` | 30 | 心跳超时兜底退出（正常关页 2~4 秒即退出） |
| `SHUTDOWN_GRACE` | 2 | 收到关闭信标后的宽限秒数，留给应用内跳转的新页面来心跳 |
| `WEAK_AC_RATE` / `STRONG_AC_RATE` | 40 / 75 | 薄弱 / 强项标签判定阈值 |
| `RECOMMEND_LIMIT` | 8 | 推荐题目数量 |
| `MIN_PROBLEM_RATING` | 800 | CF 题库最低难度分，训练区间与推荐池的下界 |
| `UNRATED_PLAN_RATING` | 1200 | 未评级用户（CF 无 rating）的训练计划基准分 |
| `HTTP_MAX_CONCURRENCY` | 4 | 全局并发请求上限（调高更快但可能触发 CF 限流，不建议 >6） |
| `USER_CACHE_TTL` | 300 | 用户数据缓存秒数（重复查询/详情命中） |
| `WALL_MIN_ATTEMPTS` | 4 | 弱点墙：某档至少尝试多少题才算可信（稳固上限与墙都受此约束） |
| `WALL_WALL_RATE` | 50 | 弱点墙：稳固上限与「墙」的共同分界线 |
| `MAX_PROBLEM_RATING` | 3500 | CF 题库真实难度上限，用于钳住刷题器深链的目标区间 |
| `PICK_DEFAULT_COUNT` / `PICK_MAX_COUNT` | 12 / 50 | 刷题器默认 / 最多出题数 |
| `TEAM_MAX_SIZE` | 6 | 组队作战一次最多分析人数 |
| `TEAM_MATRIX_TAGS` | 16 | 技能矩阵展示的标签数（按全队解题总数取 TOP） |
| `TEAM_WEAK_BEST_MAX` | 4 | 连全队最强者解题数都低于此值 → 判为共同弱项 |

---

## 📜 许可证与版权

**Copyright © 2026 [morning-cml](https://github.com/morning-cml)**

本项目依 **[GNU General Public License v3.0](LICENSE)** 授权发布。

**你可以：**

- 自由地使用、研究、修改本项目；
- 自由地再分发原版或你的修改版。

**但你必须：**

- **保留版权声明与本许可证**，注明原作者与项目出处；
- **明确标注你所做的改动**；
- 分发修改版时，**必须同样以 GPL-3.0 开源全部源码**——不得将本项目或其衍生作品闭源发布、
  或包装进闭源产品中分发。

换句话说：**你可以站在这个项目上做得更好，但那个"更好的版本"也必须开源，并注明源自这里。**

> 本项目仅调用 Codeforces 官方公开 API，不涉及账号凭据；Codeforces 及其数据的权利归其各自所有者。
> 许可证全文见 [LICENSE](LICENSE)；具体法律效力以你所在司法辖区的著作权法为准。
